package org.vista;

import javax.swing.*;

public class SiguienteVentana extends JFrame {
    public SiguienteVentana() {
        setSize(300, 200);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setVisible(true);
    }
}